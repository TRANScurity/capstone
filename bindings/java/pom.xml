<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.github.transcurity</groupId>
    <artifactId>capstone</artifactId>
    <version>5.0.9</version>
    <packaging>jar</packaging>

    <name>${project.groupId}:${project.artifactId}</name>
    <description>Java binding for Capstone disassembly framework in Maven compatible form.</description>
    <url>https://github.com/TRANScurity/capstone</url>

    <organization>
        <name>TRANScurity</name>
        <url>http://transcurity.co/</url>
    </organization>

    <developers>
        <developer>
            <name>Andreas "PAX" Lück</name>
            <email>onkelpax-maven[at]yahoo[dot]com</email>
            <organization>TRANScurity</organization>
            <organizationUrl>http://transcurity.co/</organizationUrl>
        </developer>
    </developers>

    <licenses>
        <license>
            <name>${license-title}</name>
            <url>https://opensource.org/licenses/BSD-3-Clause</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <!--
    ================== HOW TO RELEASE ==================

    1. Sign in to http://central.sonatype.org/ and generate a user + password and put it into your Maven settings.xml:

    &lt;servers&gt;
        &lt;server&gt;
            &lt;id&gt;central&lt;/id&gt;
	        &lt;username&gt;...&lt;/username&gt;
	        &lt;password&gt;...&lt;/password&gt;
        &lt;/server&gt;
    &lt;/servers&gt;

    2. If the release fails due to unkown fingerprint key, upload it to a PGP server:

    $gpg - -list-secret-keys - -keyid-format LONG

    sec   rsa4096 2024-01-01 [SC]
          YOUR_LONG_KEY_ID
    uid   Your Name <you@example.com>
    ssb   rsa4096 2024-01-01 [E]
          ...

    $gpg - -keyserver keyserver.ubuntu.com - -send-keys YOUR_LONG_KEY_ID

    3. Upload the release to Maven Central (disable Zscaler):

    $mvn clean deploy -Prelease
    -->

    <scm>
        <connection>scm:git:https://github.com/TRANScurity/capstone.git</connection>
        <developerConnection>scm:git:ssh://git@github.com/TRANScurity/capstone.git</developerConnection>
        <url>https://github.com/TRANScurity/capstone/tree/master</url>
    </scm>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.release>8</maven.compiler.release>
        <license-title>BSD license</license-title>
    </properties>

    <profiles>
        <profile>
            <id>release</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.sonatype.central</groupId>
                        <artifactId>central-publishing-maven-plugin</artifactId>
                        <version>0.9.0</version>
                        <extensions>true</extensions>
                        <configuration>
                            <publishingServerId>central</publishingServerId>
                            <autoPublish>true</autoPublish>
                        </configuration>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-source-plugin</artifactId>
                        <version>3.3.1</version>
                        <executions>
                            <execution>
                                <id>attach-sources</id>
                                <goals>
                                    <goal>jar-no-fork</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-javadoc-plugin</artifactId>
                        <version>3.11.2</version>
                        <configuration>
                            <doclint>none</doclint>
                        </configuration>
                        <executions>
                            <execution>
                                <id>attach-javadocs</id>
                                <goals>
                                    <goal>jar</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-gpg-plugin</artifactId>
                        <version>3.2.8</version>
                        <executions>
                            <execution>
                                <id>sign-artifacts</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>sign</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.4.1</version>
                <configuration>
                    <archive>
                        <manifestFile>${project.build.outputDirectory}/META-INF/MANIFEST.MF</manifestFile>
                        <index>true</index>
                        <manifest>
                            <addClasspath>true</addClasspath>
                            <addDefaultImplementationEntries>true</addDefaultImplementationEntries>
                        </manifest>
                        <manifestEntries>
                            <!-- Currently not required -->
                        </manifestEntries>
                    </archive>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.felix</groupId>
                <artifactId>maven-bundle-plugin</artifactId>
                <version>5.1.9</version>
                <executions>
                    <execution>
                        <id>bundle-manifest</id>
                        <phase>process-classes</phase>
                        <goals>
                            <goal>manifest</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>baseline</id>
                        <goals>
                            <goal>baseline</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <dependencies>
        <dependency>
            <groupId>net.java.dev.jna</groupId>
            <artifactId>jna</artifactId>
            <version>5.19.1</version>
        </dependency>
    </dependencies>
</project>
